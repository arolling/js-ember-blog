import { hasComments } from 'blog/helpers/has-comments';
import { module, test } from 'qunit';

module('Unit | Helper | has comments');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = hasComments([42]);
  assert.ok(result);
});
